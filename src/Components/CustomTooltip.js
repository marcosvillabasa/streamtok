import styled from "styled-components"

export const CustomTooltip = styled.div`
  .title {
    margin-bottom: 1em;
    font-size: 1.2em;
  }
  .description {
    margin-bottom: 1em;
    font-size: 1.1em;
  }
  .tags {
    color: var(--color-text-8);
  }
`
